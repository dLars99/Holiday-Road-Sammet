const convertAttractionObject = (attractionObject) => {
    const attractionOptionHTML = `<option value=${attractionObject.name}>${name}</option`
    console.log(attractionOptionHTML)
    return attractionOptionHTML
}